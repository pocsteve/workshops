pipeline {
    agent any
    stages {
        stage('Delete the workspace') {
            steps {
                sh "rm -rf $WORKSPACE/*"
            }
        }
        stage('Second Stage') {
            steps {
                echo "Second stage"
            }
        }
        stage('Download MongoDB') {
            steps {
                git credentialsId: 'git-repo-creds', url: 'https://github.com/pocsteve/workshops.git'
            }
        }
    }
}